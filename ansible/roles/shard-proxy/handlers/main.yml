---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  when: ansible_os_family != 'FreeBSD'

- name: Restart shard-proxy
  ansible.builtin.service:
    name: "{{ 'shard-proxy' if ansible_os_family != 'FreeBSD' else 'shard_proxy' }}"
    state: restarted
